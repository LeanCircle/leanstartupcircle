- title "Meetups Index | Lean Startup Circle"

#index_meetups
  .row
    .span12
      = gmaps("markers" => { "data" => @meetups, :options => { :randomize => true, :max_random_distance => 1000, :list_container => "meetups" } })
      %table.table.table-condensed.table-striped.meetups
        %caption
          %h1{ :style => "display: inline"}
            Lean Startup Meetups Worldwide
          %span.pull-right
            = link_to "Add a Meetup", new_meetup_path, :class => "btn btn-primary"
        %thead
          %tr
            %th Name
            %th City
            %th State
            %th Country
            %th Links
        %tbody#meetups