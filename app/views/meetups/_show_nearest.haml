- location = request.location
- meetup = Meetup.near(location.coordinates, 100).first

.nearest_meetup
  You are in
  = location.address
  Nearest meetup?
  - if meetup.blank?
    None,
    = link_to "start a new meetup!", "http://leanstartup.pbworks.com/w/page/55605569/Organizer's%20Guide"
  - else
    = link_to meetup.link do
      #{ meetup.name } - #{ meetup.address }
