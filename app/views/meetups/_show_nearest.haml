.nearest_meetup
  You are in
  = @location.address
  Nearest meetup?
  - if @nearest_meetups.blank?
    None,
    = link_to "start a new meetup!", "http://leanstartup.pbworks.com/w/page/55605569/Organizer's%20Guide", :target => '_blank'
  - elsif @nearest_meetups.count == 1
    = link_to meetup.link do
      #{ meetup.name } - #{ meetup.address }
  - else
    There are
    = link_to @nearest_meetups.count.to_s + " near you!", meetups_url(:subdomain => 'meetups')

