.nearest_meetup.pull-right
  - if @nearest_meetups.blank?
    There are no meetups near you.
    = link_to "Start a group now!", "http://leanstartup.pbworks.com/w/page/55605569/Organizer's%20Guide", :target => '_blank'
  - else
    Join
    = link_to  @nearest_meetups.first.name + " - " +@nearest_meetups.first.address_no_country, @nearest_meetups.first.link, :target => '_blank'
    - if @nearest_meetups.count != 1
      or
      = link_to (@nearest_meetups.count - 1).to_s + " other groups near you!", meetups_url(:subdomain => 'meetups')